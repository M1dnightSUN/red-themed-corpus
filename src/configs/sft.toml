[paths]
model_path = "D:/models/Qwen2.5-7B-Instruct"
input_json = "data/red_dataset.json"
sft_data_dir = "data/sft"
output_dir = "data/ckpt_sft"

[data]
eval_ratio = 0.02
seed = 42
max_items = -1

[train]
max_seq_len = 2048
per_device_batch_size = 2
grad_accum_steps = 8
learning_rate = 2e-4
num_train_epochs = 1
warmup_ratio = 0.03
lr_scheduler_type = "cosine"
logging_steps = 20
save_steps = 500
eval_steps = 500
save_total_limit = 2
packing = true
lora_r = 16
lora_alpha = 16
lora_dropout = 0.0
resume = true
